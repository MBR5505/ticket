<!DOCTYPE html>
<html lang="no">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Feil - Helpdesk Billettsystem</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .error-container {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 20px;
    }
    
    .error-icon {
      font-size: 80px;
      color: var(--danger-color);
      margin-bottom: 20px;
    }
    
    .error-title {
      font-size: 32px;
      margin-bottom: 10px;
      color: var(--danger-color);
    }
    
    .error-message {
      font-size: 18px;
      margin-bottom: 30px;
      max-width: 600px;
      color: var(--dark-color);
    }
    
    .error-details {
      background-color: rgba(0,0,0,0.05);
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 30px;
      max-width: 800px;
      text-align: left;
      overflow: auto;
    }
    
    .error-actions {
      display: flex;
      gap: 10px;
    }
    
    .error-code {
      position: absolute;
      bottom: 20px;
      opacity: 0.5;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="error-icon">
      <i class="fas fa-exclamation-triangle"></i>
    </div>
    
    <h1 class="error-title">Oi, noe gikk galt!</h1>
    
    <p class="error-message">
      <%= message || 'Det oppstod en feil under behandling av forespørselen din.' %>
    </p>
    
    <% if (error && error.stack && process.env.NODE_ENV !== 'production') { %>
      <div class="error-details">
        <pre><%= error.stack %></pre>
      </div>
    <% } %>
    
    <div class="error-actions">
      <a href="javascript:history.back()" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Gå tilbake
      </a>
      <a href="/" class="btn btn-primary">
        <i class="fas fa-home"></i> Gå til startsiden
      </a>
    </div>
    
    <% if (status) { %>
      <div class="error-code">Error <%= status %></div>
    <% } %>
  </div>
</body>
</html>
